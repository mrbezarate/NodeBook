[package]
name = "brain-charts"
version = "0.1.0"
edition = "2021"

[dependencies]
plotters = { version = "0.3.5", default-features = false, features = ["bitmap_backend", "bitmap_encoder", "ttf", "image"] }
brain-common = { path = "../brain-common" }
chrono = "0.4"
png = "0.18.1"
